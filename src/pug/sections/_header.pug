header.grid-container.header
    +menu(['--header', 'simple'])
    if page == 'main'
        +menu(['--lines', '--gray'], [{
            content: 'RESEARCH',
            link: '#'
        },
        {
            content: 'DATA ANALYTICS',
            link: '#',
            state: 'active'
        }])